<section>
    <div class="group-wrapper">
        <% @comments.each do |c| %>
            <div>
                <%=c.body%><br>
            </div>
        <% end %>
        <h3>投稿する</h3>
        <%= form_for :comment, url: comments_path do |f| %>
            <%= f.text_area :body, placeholder: '投稿内容' %>
            <%= f.hidden_field :group_id, :value => @current %> 
            <%= f.hidden_field :student_id, :value =>  @current_student.id %>
            <%= f.submit%>
        <% end %>
    </div>    
    </div>
</section>